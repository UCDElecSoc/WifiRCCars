cd `~Documents\GitHub\makerlab-remote-control>`
Activate venv `.\.venv\Scripts\Activate`
Run `uvicorn app.main:app --app-dir server --reload --host 0.0.0.0 --port 8000`

cd `frontend` and `npm install`. Then `npm run dev`

Go to '